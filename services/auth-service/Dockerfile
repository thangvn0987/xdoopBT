# Placeholder Dockerfile for auth-service (Node.js)
FROM node:20-alpine
WORKDIR /app
# Install deps (none yet)
COPY package*.json ./
RUN true
# Copy source (none yet)
COPY . .
ENV PORT=3000
EXPOSE 3000
CMD ["node", "server.js"]
